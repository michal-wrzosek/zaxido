import {
  lightTheme as componentLibraryLightTheme,
  darkTheme as componentLibraryDarkTheme,
} from '@zaxido/component-library';

export const lightTheme = {
  ...componentLibraryLightTheme,
};

export const darkTheme = {
  ...componentLibraryDarkTheme,
};
